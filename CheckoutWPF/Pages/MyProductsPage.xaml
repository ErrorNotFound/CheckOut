<Page x:Class="CheckoutWPF.Pages.MyProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CheckoutWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="718" 
      d:DesignWidth="1024"
      Title="MyProductsPage">
    
    <Page.DataContext>
        <local:MyProductsPageViewModel />
    </Page.DataContext>

    <Grid>
        <!--
        <TabControl ItemsSource="{Binding Source={x:Static data:ProductRepository.Instance}, Path=Categories}"
                        Margin="0 10 0 0"
                        Background="{DynamicResource BrushColor3}"
                        Style="{DynamicResource MaterialTabControlStyle}"
                        ItemContainerStyle="{DynamicResource MaterialTabControlItemStyle}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name, Converter={StaticResource stringToUppercaseStringConverter}}"
                                   Foreground="{DynamicResource BrushColorFontDark}" 
                                   TextOptions.TextFormattingMode="Ideal"
                                   FontSize="15"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ItemsControl Background="{DynamicResource BrushColor3}">
                        <ItemsControl.ItemsSource>
                            <MultiBinding Converter="{StaticResource observableListToCollectionViewConverter}" Mode="OneWay">
                                <Binding Source="{x:Static data:ProductRepository.Instance}" Path="Products" />
                                <Binding Path="Id" />
                            </MultiBinding>
                        </ItemsControl.ItemsSource>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Tag="{Binding Name}"
                                            Style="{StaticResource TileStyle}"
                                            Command="{Binding RelativeSource={RelativeSource AncestorType=Page}, Path=DataContext.AddProductCommand}"
                                            CommandParameter="{Binding Id}">
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
        -->
    </Grid>
</Page>
